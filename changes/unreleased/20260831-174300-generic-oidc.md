---
type: breaking
---

OIDC is no longer Authentik-specific: set `OIDC_CLIENT_ID`, `OIDC_CLIENT_SECRET`, and `OIDC_DISCOVERY_URL`, and register redirect URI `/api/auth/callback/oidc` on the IdP. `AUTHENTIK_*` app env vars and `/api/auth/callback/authentik` are no longer used.
