# Backend host for tRPC HTTP + WebSocket.
# Empty = same origin (production when the backend serves the SPA).
VITE_BACKEND_URL=http://localhost:3101

# Optional. Defaults to root package.json "version" at build time.
# VITE_APP_VERSION=0.1.0
